{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Debug Express Server",
      "program": "${workspaceFolder}/app.js",  // change to your main file
      "env": {
        "NODE_ENV": "development"
      }
    }
  ]
}
